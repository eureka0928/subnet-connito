# -----------------------------
# PyTorch (install CUDA wheel from pytorch.org first)
# -----------------------------
torch==2.10.0

# -----------------------------
# Core & Utilities
# -----------------------------
pydantic-settings==2.2.1
structlog==24.1.0
prometheus-client==0.20.0
psutil==6.1.1
PyYAML==6.0.1
typer==0.24.1
python-dotenv==1.1.0
cryptography==46.0.5
varint==1.0.2
multihash==0.1.1

# -----------------------------
# ML stack
# -----------------------------
transformers==5.4.0
datasets==3.0.0
huggingface_hub==1.10.1
torchdata==0.11.0
scipy==1.15.3
prefetch_generator==1.0.3
unsloth @ git+https://github.com/unslothai/unsloth.git; platform_system == "Linux"

# -----------------------------
# API / server (validator)
# -----------------------------
fastapi==0.110.1
uvicorn[standard]==0.30.0
requests==2.32.3
aiofiles==25.1.0
python-multipart==0.0.22

# -----------------------------
# Distributed training
# -----------------------------
git+https://github.com/learning-at-home/hivemind.git@4bd43b77
py-cid==0.5.0

# -----------------------------
# Storage / filesystem
# -----------------------------
fsspec==2024.6.1

# -----------------------------
# Blockchain
# -----------------------------
bittensor==10.1.0
bittensor-cli==9.18.0
# Pinned to pre-cyscale release. Newer async-substrate-interface versions
# migrated scalecodec → cyscale and added an import-time check that fails
# when bittensor-cli's transitive substrate-interface also pulls scalecodec.
async-substrate-interface==1.6.4

# -----------------------------
# Observability
# -----------------------------
wandb==0.25.1
numpy==2.0.2
pandas==2.2.3

# ------------------------------
# Dev & Testing
# ------------------------------
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
hypothesis==6.112.0

ruff==0.6.3
black==24.8.0
mypy==1.11.1
types-PyYAML==6.0.12.20240808

# Grafana dashboard generation (run `generate-dashboard` to emit JSON).
grafanalib==0.7.1